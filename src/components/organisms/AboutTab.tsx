import React from "react";
import { Box, Typography } from "@mui/material";

export const AboutTab: React.FC = () => (
  <Box sx={{ mt: 3 }}>
    <Typography variant="body1">User about info will go here...</Typography>
  </Box>
);
